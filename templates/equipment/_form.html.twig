<div class="card">
  <div class="card-body">
    {{ form_start(form) }}
      {{ form_widget(form) }}
      <div class="mt-3 d-flex gap-2">
        <button class="btn btn-outilne-primary"><i class="fa-solid fa-floppy-disk me-1"></i>{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_equipment_index') }}" class="btn btn-outilne-danger"><i class="fa-solid fa-xmark me-1"></i>Annuler</a>
      </div>
    {{ form_end(form) }}
  </div>
</div>
