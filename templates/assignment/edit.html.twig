{% extends 'base.html.twig' %}

{% block title %}Éditer affectation{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="h3 m-0">Éditer affectation</h1>
        <a href="{{ path('app_assignment_index') }}" class="btn btn-outilne-primary"><i class="fa-solid fa-arrow-left me-1"></i>Retour</a>
    </div>

    {{ include('assignment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="mt-3">
        {{ include('assignment/_delete_form.html.twig') }}
    </div>
{% endblock %}
