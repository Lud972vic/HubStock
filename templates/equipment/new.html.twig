{% extends 'base.html.twig' %}

{% block title %}Nouveau matériel{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="h3 m-0">Nouveau matériel</h1>
        <a href="{{ path('app_equipment_index') }}" class="btn btn-outilne-primary"><i class="fa-solid fa-arrow-left me-1"></i>Retour</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('equipment/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>
    </div>
{% endblock %}
