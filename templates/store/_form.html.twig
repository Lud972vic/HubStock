<div class="card">
  <div class="card-body">
    {{ form_start(form) }}
      {{ form_row(form.name) }}
      {{ form_row(form.address) }}
      {{ form_row(form.sr) }}
      {{ form_row(form.codeFR) }}
      {{ form_row(form.statut) }}
      {{ form_row(form.typeDeProjet) }}
      {{ form_row(form.dateOuverture) }}
      <div class="mt-3 d-flex gap-2">
        <button class="btn btn-outilne-primary"><i class="fa-solid fa-floppy-disk me-1"></i>{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_store_index') }}" class="btn btn-outilne-danger"><i class="fa-solid fa-xmark me-1"></i>Annuler</a>
      </div>
    {{ form_end(form) }}
  </div>
</div>
