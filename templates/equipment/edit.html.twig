{% extends 'base.html.twig' %}

{% block title %}Modifier le matériel{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="h3 m-0">Modifier le matériel</h1>
        <a href="{{ path('app_equipment_index') }}" class="btn btn-outilne-primary"><i class="fa-solid fa-arrow-left me-1"></i>Retour</a>
    </div>

    <div class="card mb-3">
        <div class="card-body">
            {{ include('equipment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div>
        {{ include('equipment/_delete_form.html.twig') }}
    </div>
{% endblock %}
