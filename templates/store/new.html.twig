{% extends 'base.html.twig' %}

{% block title %}Nouveau magasin{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="h3 m-0">Nouveau magasin</h1>
        <a href="{{ path('app_store_index') }}" class="btn btn-outilne-primary"><i class="fa-solid fa-arrow-left me-1"></i>Retour</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('store/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>
    </div>
{% endblock %}
